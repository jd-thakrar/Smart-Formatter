<?xml version="1.0"?>
<project name="SmartFormatterPro" default="run" basedir=".">

    <!-- === Project properties === -->
    <property name="src" value="src"/>
    <property name="build" value="build/classes"/>
    <property name="dist" value="dist"/>
    <property name="main-class" value="com.smartformatter.GUI"/>

    <!-- === Classpath === -->
    <!-- 👇 This now points to Libraries instead of lib -->
    <path id="classpath">
        <fileset dir="Libraries">
            <include name="*.jar"/>
        </fileset>
    </path>

    <!-- === Clean === -->
    <target name="clean">
        <echo>🧹 Cleaning build and dist folders...</echo>
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
    </target>

    <!-- === Compile === -->
    <target name="compile">
        <echo>🧩 Compiling source files...</echo>
        <mkdir dir="${build}"/>
        <javac srcdir="${src}" destdir="${build}" includeantruntime="false" debug="true" encoding="UTF-8">
            <classpath refid="classpath"/>
        </javac>
    </target>

    <!-- === Create JAR === -->
    <target name="jar" depends="compile">
        <echo>📦 Creating SmartFormatterPro.jar...</echo>
        <mkdir dir="${dist}"/>
        <jar destfile="${dist}/SmartFormatterPro.jar" basedir="${build}">
            <manifest>
                <attribute name="Main-Class" value="${main-class}"/>
                <attribute name="Class-Path" 
                    value="Libraries/openpdf-3.0.0.jar Libraries/poi-5.2.5.jar Libraries/poi-ooxml-5.2.5.jar Libraries/commons-collections4-4.4.jar"/>
            </manifest>
        </jar>
        <echo>✅ JAR created successfully at: ${dist}/SmartFormatterPro.jar</echo>
    </target>

    <!-- === Run Application === -->
    <target name="run" depends="compile">
        <echo>🚀 Running SmartFormatterPro...</echo>
        <java classname="${main-class}" fork="true">
            <classpath>
                <path refid="classpath"/>
                <pathelement location="${build}"/>
            </classpath>
        </java>
    </target>

</project>
